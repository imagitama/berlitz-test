import styled from '@emotion/styled'
import sizes from '../../sizes'

const ProductTitle = styled.h1({
  fontSize: '2rem',
  fontWeight: 'bold',
  margin: `${sizes.large} 0 0`
})

export default ProductTitle